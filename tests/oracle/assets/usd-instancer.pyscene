# UsdGeomPointInstancer (see usd-instancer.usda).
sceneBuilder.importScene('usd-instancer.usda')

camera = Camera()
camera.position = float3(0, 1, 9)
camera.target = float3(0, 0.3, 0)
camera.up = float3(0, 1, 0)
sceneBuilder.addCamera(camera)
sceneBuilder.selectedCamera = camera
