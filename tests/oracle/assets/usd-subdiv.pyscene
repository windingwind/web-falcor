# Subdivision surfaces (see usd-subdiv.usda).
sceneBuilder.importScene('usd-subdiv.usda')

camera = Camera()
camera.position = float3(0, 2, 6)
camera.target = float3(0, 0, 0)
camera.up = float3(0, 1, 0)
sceneBuilder.addCamera(camera)
sceneBuilder.selectedCamera = camera
