# Time-sampled USD curves (see usd-curves-anim.usda).
sceneBuilder.importScene('usd-curves-anim.usda')

camera = Camera()
camera.position = float3(2.5, 2.0, 8.0)
camera.target = float3(2.5, 2.0, 0.4)
camera.up = float3(0, 1, 0)
sceneBuilder.addCamera(camera)
sceneBuilder.selectedCamera = camera
