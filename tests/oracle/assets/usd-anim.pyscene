# Time-sampled USD transforms (see usd-anim.usda).
sceneBuilder.importScene('usd-anim.usda')

camera = Camera()
camera.position = float3(0, 1.5, 6)
camera.target = float3(0, 0, 0)
camera.up = float3(0, 1, 0)
sceneBuilder.addCamera(camera)
sceneBuilder.selectedCamera = camera
