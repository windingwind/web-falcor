# web-falcor — Feature parity matrix & divergences

Part of the [web-falcor design docs](README.md). Section numbers (§8–§9) are
kept stable across the split so the `§N` cross-references throughout the docs
stay valid. The ✅ / 🟡 / 🔶 / ⏳ / ❌ / 🟠 status markers are defined in
[architecture.md §1](architecture.md#1-goals-and-ground-rules).

Every row below was verified against the actual code (2026-07-09 audit of
`packages/` vs upstream `Falcor/Source/`, rows re-verified as features landed
through 2026-07-12), not against intent: ✅/🟡/🔶 mean the
thing exists and works today, ⏳ means portable-but-not-built, 🟠 means blocked
by a documented toolchain/asset gap, ❌ means the web platform cannot provide it.

## 8. Feature parity matrix

### 8.1 Platform / Core capabilities

| Feature | Status | Explanation / strategy |
|---|---|---|
| D3D12 / Vulkan backends | 🔶 | WebGPU is the backend (itself lowered to D3D12/Vulkan/Metal by the browser) |
| WebGL2 backend | 🔶 partial-by-design | raster-only subset possible (no compute in WebGL2); deferred, see §2 |
| Slang shading language, full library | ✅ | Slang WGSL backend; unmodified upstream `.slang` tree compiled in-browser (slang-wasm) with a small override set |
| Runtime shader specialization (DefineList) | ✅ | slang-wasm in-browser compilation + AOT cache |
| Type conformances (first-class API) | 🟡 | `ProgramDesc.typeConformances` / `ComputePassDesc.typeConformances` mirror `ProgramDesc::addTypeConformances` (type, interface, ID). slang-wasm has no `createTypeConformanceComponentType`, so the compiler lowers each `createDynamicObject<I, T>(id, data)` in a compiled module to a generated switch that reinterprets `data` as the type registered for `id`, which is what the conformance table does natively; lowered modules get their own session name/path. Verified by the transplanted Inheritance_ConformanceCreate and ShaderStringDynamicObject FalcorTests. Materials keep `WEBFALCOR_MTL_*` static specialization. §9: an unregistered ID falls back to the lowest-ID conformance instead of undefined behaviour |
| Shader reflection → ParameterBlock/ShaderVar | ✅ | slang reflection JSON / wasm API; ShaderVar folded into `ParameterBlock.makeRootVar()`, with `var["x"].getBuffer()` read-back. Storage textures take their bound textures' formats like native UAVs: Slang infers a WGSL storage format from the element type (float4 → rgba32float, uint4 → rgba32uint), so ComputePass retargets the declaration to the bound texture's format (same texel type) and rebuilds its pipeline (`StorageTextureFormat` test; it is why native Composite writes the adaptive graph's R32Uint density map unmodified). `StandaloneParameterBlock.create(device, getReflector().getParameterBlock(name))` mirrors `ParameterBlock::create` from reflection: filled on its own, then bound with `var["gBlock"] = block`. §9: program blocks are laid out per program here, so the standalone block records its values and forwards them, later writes included, to every program it is bound to |
| Hardware RT (DXR pipelines, inline RayQuery, SBTs) | 🟡 | **No WebGPU ray tracing API exists.** Software CPU-built BVH + compute traversal + megakernel lowering (§5); semantics preserved, performance lower. Hardware TLAS/BLAS, compaction, refit, `RtStateObject`, `ShaderTable` themselves ❌ |
| Shader Execution Reordering (NVAPI) | ❌ | NVIDIA hardware/driver feature; no web analog. No-op shim (perf-only) |
| Wave/subgroup intrinsics | 🟡 | WebGPU `subgroups` feature where available; workgroup-shared fallback |
| 64-bit shader integers/atomics | 🟡 | not in WGSL; paired-u32 emulation shim (verified bit-identical: SplitMix64 seeding, xoshiro128** streams) |
| fp16 in shaders | 🟡 | Chromium does not expose `shader-f16` on this host (driver supports it): token-level f16→f32 demotion; f16 rounding only at pack boundaries. Host-side packs (materials, vertices, emissive UVs, alias tables) use `Utils/Math/Float16.ts`, a bit-faithful port of native `Float16.cpp` (round to nearest, ties up) — an earlier truncating pack biased material params by up to one fp16 ULP (found by the BSDFIntegrator oracle). 16-bit ints demoted likewise (absent from WGSL entirely) |
| fp64 in shaders | ❌ | absent from WGSL entirely; native's few uses switch to compensated-f32 🟡 (e.g. the watertight-triangle tiebreak in `IntersectionHelpers.slang` → Kahan difference-of-products via `fma`). Caution: fp64 reaching slangc's WGSL backend aborts emission *silently* — the compiler now fails loudly on empty entry points |
| Bindless resources / unbounded descriptor arrays | 🟡 | not in browser WebGPU; texture-array packing per format class (§6.2), documented limits |
| Min/max-reduction texture samplers | 🟡 | D3D12 `TextureReductionMode` has no WebGPU equivalent. Sole upstream consumer (displacement shell tightening) is overridden to conservative global bounds — identical intersections (§9.7); host-built min/max mip pyramids are the substitute if tight bounds are ever needed. RenderContext.blit now takes native's per-channel reduction modes (the complex blit) and computes min/max over the texels the filter footprint touches, which is what a reduction sampler returns, so `Texture.generateMips(ctx, minMaxMips)` produces native's average/min/max/average mips (the transplanted TextureMinMaxMip matches byte for byte). Sampler objects with Min/Max reduction still throw |
| Indirect dispatch | ✅ | `dispatchWorkgroupsIndirect` wired (`ComputeContext.dispatchRawIndirect`) |
| Indirect draw / ExecuteIndirect | ✅ | `RasterPass.drawIndirect`/`drawIndexedIndirect` (GPU-driven args verified incl. zero-count command); §9: no GPU count buffer in WebGPU — multi-command loops over the arg stride |
| UAV counters / append buffers | 🟡 | emulated with explicit atomic counter buffers (packed-region pattern, see PixelStats) |
| GpuTimer / timestamp queries | ✅ | standard `timestampWrites` on every compute/render pass via `Core/API/Profiler.ts` (RenderGraph labels each pass); legacy `Core/API/GpuTimer.ts` (`writeTimestamp`) kept for ad-hoc use |
| Profiler framework (FALCOR_PROFILE, Clock/FrameRate/TimeReport) | ✅ core | `Core/API/Profiler.ts` ports the native event model: nested `startEvent`/`endEvent` names (`/RenderGraphExe::execute()/<pass>`), CPU time per event, GPU time from pass `timestampWrites` attributed to every active event (parents include children like native GpuTimer spans; verified parent == Σ children), EMA averages (σ 0.98), 512-frame history stats, pause, multi-frame capture → native JSON lanes; `ProfilerUI` DOM panel (Event / CPU / CPU % / GPU / GPU % table, bars, stacked history graph, Start/End Capture download) toggled with P in Mogwai; `m.profiler` python binding (`enabled`/`paused`/`events`/`start_capture`/`end_capture`); Clock ✅ (`m.clock`), FrameRate ✅, TimeReport ✅. §9: GPU readback lands a frame or two late and frames beyond the 4-deep readback ring are dropped rather than fenced; enabled by default; no `with profiler.event()` context manager (no PIX markers) |
| Occlusion queries | ✅ | `Core/API/QueryHeap.ts` (occlusion + timestamp types) + `RasterPass.setOcclusionQuery`; verified: depth-rejected draw reads 0 samples, visible draw counts all. Pipeline-statistics queries ❌ (not in WebGPU) |
| Async compute / multiple queues | ❌ | WebGPU exposes a single queue; Falcor's LowLevelContextData queue selection becomes a no-op (correctness unaffected) |
| CUDA interop (buffers, semaphores, PyTorch tensors) | ❌ | no CUDA in browsers, full stop. `CudaUtils`/`CudaInterop` throw `UnsupportedFeatureError` |
| NSight Aftermath | ❌ | driver crash-dump tech; browser substitute is WebGPU validation + device-lost logs |
| Multi-GPU / LUID adapter selection | ❌ | browser picks adapter; only `powerPreference` hint exposed |
| Exclusive fullscreen / vsync control / HDR swapchain | 🟡 | Fullscreen API + canvas `toneMapping` (HDR in Chrome); no vsync-off, no refresh-rate control |
| Memory-mapped files, raw file paths, process spawn, registry/env | ❌ | sandboxed platform; OPFS + File System Access + fetch replace file I/O |
| Shader hot reload (in-app `reloadShaders`) | ✅ | `ProgramManager.reloadAllPrograms()` mirrors native: every compiled version is dropped and passes rebuild their kernels and pipelines on their next frame, so the scene, camera and render graph survive. The viewer re-fetches the shader tree and reloads on **F6**. §9: native uses F5, which the browser owns. Verified on GPU: a pass created before the reload runs an edited shader afterwards, and reloading every program under a live raster + ray-traced graph gives byte-identical output |
| Multithreaded scene build (TaskManager) | ⏳ | scene build is single-threaded today. Measured on a 131k-triangle scene: the software-RT BVH was two thirds of it (364ms of 542ms), now 153ms after the builder moved onto flat index arrays, so the remaining parallel win is ~100ms there. glTF image decoding now overlaps. Actually threading it needs Web Workers *and* an async Scene construction path — `new Scene(...)` builds the BVH in its constructor — so it is an API change, not just a worker pool |
| Plugin system (dynamic pass/importer loading) | 🔶 | native .dll/.so loading ❌; passes register via a static factory + side-effect import; `loadRenderPassLibrary(url)` dynamic-imports a JS module that calls `registerRenderPass` (bare `@web-falcor/falcor` import or the `globalThis.webFalcorPlugins` hook for blob:/data: modules) and returns the added types — importer plugins ⏳ |
| Settings system (global `Settings`, attribute filters) | ✅ | `Utils/Settings.ts`: colon-flattened options, ordered attribute filters (full-match regex, nested flatten, deprecated `.filter` + negation), `searchpath:`/`standardsearchpath:` directory categories (Renderman `;`/`@`/`&` list semantics via `Utils/PathResolving.ts`; `searchpath:media` feeds the default AssetResolver like SampleApp), `m.settings` python binding, `RenderPass.onOptionsChange` hook; §9: no settings.json autoload, `${ENV}` expands to empty |

### 8.2 Render passes (29 upstream directories, 38 registered pass classes)

Tallies today: 21 pass classes fully implemented, 4 partial, 13 not implemented
(of which 4 ❌ NVIDIA-SDK-bound, 2 🟠 autodiff-blocked).

| Pass | Status | Notes |
|---|---|---|
| AccumulatePass | ✅ | `Double` mode maps to SingleCompensated (fp64 gap 🟡); `maxFrameCount` + `overflowMode` (Stop / Reset / EMA) honoured like native (moving-average kernel mode, count stops at the limit; not in SingleCompensated, like native) |
| BlitPass | ✅ | |
| BSDFOptimizer | 🟠 | no host port; depends on the same slangc autodiff blocker as WARDiffPathTracer (§6.9), plus an optimizer loop |
| BSDFViewer | ✅ | verified vs native (1.9e-4); Slice viewer mode (`viewerMode`) exposed on the host |
| DebugPasses: ColorMapPass / SideBySidePass / SplitScreenPass | ✅ | verified; interactive divider ✅ (native `onMouseEvent`: hover highlight within max(6, dividerSize) px, left-drag moves it, double click recenters, "Show Arrows" — the viewer forwards canvas mouse events to passes before the camera controller, like `Renderer::onMouseEvent`); `showTextLabels`/`leftLabel`/`rightLabel` ✅ via the TextRenderer port (native placement: row h−32, 16 px off the divider, 9 px per glyph) |
| DebugPasses: InvalidPixelDetectionPass | ✅ | unmodified upstream shader; functional GPU test (injected NaN→red, Inf→green, valid→black) |
| DLSSPass | ❌ | NVIDIA NGX driver + hardware black box; nearest substitutes: TAA-upscale ✅ or FSR2-WGSL port 🔶 (separate pass, not DLSS parity) |
| ErrorMeasurePass | ✅ | difference kernel (WTexture2D override — rgba32float storage is write-only) + GPU-reduced mean error; reference from input or file (EXR/HDR/browser formats); running-error EMA (one step per landed measurement) + `renderUI`. Measurements surface on `measurements`/`runningError` via async readback — csv file output ❌ (no file IO); `UseLoadedReference` toggle + `getProperties` (native key spellings) |
| FLIPPass | ✅ | LDR path verified vs native (byte MSE 6.7e-5); HDR auto-exposure (reference-luminance median/max → exposure range, async readback §9 — same 1-frame latency as native's member-after-cbuffer-write order) + pooled avg/min/max FLIP via ParallelReduction, both pinned by GPU test vs CPU recomputation |
| GBufferRaster | ✅ | all 16 native channels (kGBufferChannels + the ROV "extra" channels) — WebGPU has no rasterizer-ordered views, so connected channels are batched into ≤8 render targets and the geometry is drawn once per batch (§9.15); every channel cross-checked vs GBufferRT on the Cornell box (posW 8e-5, normals/tangents/texC/mtlData exact on 99%+ of hits); `useAlphaTest`/`adjustShadingNormals` options + controls (native defaults). Native oracle impossible on this host (ROV). `vbuffer` channel ✅ via a vertex-pulling variant (connecting it switches all batches to non-indexed pulled draws — header exact, barycentrics ≤2e-3 vs GBufferRT); native `forceCullMode` / `cull` options + controls (§9: culls nothing unless forced) |
| GBufferRT | 🟡 | SoftwareRT; verified incl. texGrads (byte-exact) |
| VBufferRT | 🟡 | SoftwareRT; verified |
| VBufferRaster | ✅ | non-indexed vertex-pulling raster (§9: WGSL lacks fragment barycentrics/primitive id — per-corner varyings + flat triangle index instead); cross-verified vs VBufferRT on cornell (1/50175 hit-pixel diff, 0 barycentric outliers); `mvec`/`mask` extra channels as render targets (§9.15; mvec matches VBufferRT to 2e-3 after a camera move, mask exact); native `forceCullMode` / `cull` options + controls (§9: culls nothing unless forced) |
| ImageLoader | ✅ | browser-decodable formats + `.hdr` + `.dds`/BC + `.exr` (parse-exr; GPU-verified exact vs CPU decode); mips/mipLevel/arrayIndex/outputSize(Fixed = image size)/outputFormat honoured (were accepted-but-ignored) |
| MinimalPathTracer | ✅ | SoftwareRT megakernel; oracle-verified (9.5e-7, §7.1); shades triangle, curve, displaced-triangle and SDF hits |
| ModulateIllumination | ✅ | lives under `Utils/` in the web tree |
| NRDPass | 🟠 SDK absent | NRD SDK not bundled in this Falcor drop (no `external/packman/nrd/`) → denoiser shaders uncompilable here. Host portable; NRD's HLSL source is public, genuine port stays the plan (§11.4). SVGF ✅ meanwhile |
| OptixDenoiser | ❌ | requires CUDA+OptiX. Same substitutes as NRD |
| OverlaySamplePass | ❌ | demo draws via raw ImGui draw lists (no web ImGui); closest equivalent would be DOM overlays — not a 1:1 port target |
| PathTracer | ✅ verified | full upstream loop: NEE+MIS, Uniform/Power/LightBVH emissive samplers, EnvMapSampler, dielectrics/nested priority, guide outputs, adaptive spp (`sampleCount` input), rayCount/pathLength stats. Fixed spp 1–16 + variable spp verified (spp=4 vs native: 10/65536 bad px); curve geometry (`USE_CURVES` + Hair BSDF) verified vs native. `USE_RTXDI` in-tracer ReSTIR direct lighting verified vs native (16-frame temporal reservoir chain, meanAbs 8.9e-4, 42 bad px; needed two storage-buffer-budget moves — see §9). Remaining ⏳: NRD guide outputs; SER ❌ |
| PixelInspectorPass | ✅ | pixel/material inspector: PixelData record via async readback (§9) + `renderUI` panel; two overrides (`ShadingData sd = {}` frontend error, `this = {};` WGSL abort); functional GPU test cross-checks the record against the G-buffer inputs; viewer canvas click-to-select wired (§8.3) |
| RenderPassTemplate | ✅ | authoring skeleton at `render-passes/src/RenderPassTemplate.ts` (registered; pass-through verified in a graph) |
| RTXDIPass | ✅ verified vs native | Full port (PrepareSurfaceData + ReSTIR spatiotemporal resampling + FinalShading). Upstream RTXDI.py replica over Arcade at frames 1/16/64: bias <6e-4, ≤5/3600 bad 8x8 blocks. Overrides: texel buffers → structured, boiling filter compiled out (WaveActiveCountBits; native default off), bool cbuffer members → uint, outputs moved into the FinalShading block (4-bind-group cap), lightInfo+compactLightInfo merged (16-storage-buffer cap); options UI ✅ (`RTXDIPass.renderUI` mirrors RTXDI::renderUI; edits recreate the context like setOptions) |
| SceneDebugger | ✅ | verified (1.0e-5) |
| SDFEditor | 🔶 core + 🟠 oracle | the editable-SDF half is built: `Scene/SDFs/SDFGridPrimitives.ts` mirrors `SDFGrid::setPrimitives/addPrimitives/removePrimitives/updatePrimitives` and the bake bookkeeping over stable primitive IDs, and `Scene.updateSDFGrids()` re-bakes an edited grid and drops the GPU resources built from it so the next bind rebuilds them. Verified on GPU: adding a primitive to a loaded `.sdf` grid puts new surface exactly where the edited CSG expression says (4.9e-3 over a 64³ grid) and removing it restores the original silhouette pixel-count-exactly. §9: upstream re-bakes incrementally on the GPU and only SDFSBS takes primitives, so baking here only fixes which primitives stay removable — the field is the same either way. What remains is the interactive pass itself (~3.9k lines of picking, 2D markers, selection wheel and GUI). Upstream flagship scene asset absent from the media drop, so native oracle impossible anyway |
| SimplePostFX | ✅ | verified |
| SVGFPass | ✅ ported | feature-verified vs native (sphere_array graph, mean 4.2e-4); all 5 kernels verbatim. Default denoiser (replacing NRD/Optix use-cases) |
| TAA | ✅ ported | feature-verified vs native (jittered GBufferRT graph, mse 8.4e-7) |
| TestPasses: TestRtProgram | ✅ | `mode` 0/1, `output`, and `addCustomPrimitive`/`removeCustomPrimitive`/`moveCustomPrimitive` (the shared mt19937 included). The shader table becomes explicit selection in a compute kernel: ray type and miss index pick colours, any-hit shaders become candidate filters in the software BVH traversal (`traverseBvhFiltered<ITriangleHitFilter>`, running only on non-opaque geometry as the BLAS Opaque flag implies), and the inscribed-sphere intersection shader tests every custom primitive. Native per-mesh geometry IDs come from `Scene.getMeshIDs()`. Verified per pixel against the native rules on analytic spheres |
| TestPasses: TestPyTorchPass | ❌ | CUDA + PyTorch tensor interop |
| ToneMapper | ✅ | all 6 operators + manual exposure verified; auto-exposure (log-luminance mip chain) verified vs native (sRGB MSE 2.1e-4, zero mean bias); fNumber/shutter/filmSpeed physical exposure verified (upstream test variants, sub-byte bias); native option set completed: exposure mode (Aperture/Shutter priority) + exposure value, white balance (CAT02 von Kries port of `Utils/Color/ColorUtils.h`, D65 preserved at 6500 K) + white point, ReinhardModified white luminance / HableUc2 linear white as options; `useSceneMetadata` accepted (no camera metadata on web scenes yet) |
| Utils (Composite/CrossFade/GaussianBlur) | ✅ | verified |
| WARDiffPathTracer | 🟠 compiler-blocked | §6.9: autodiff primitive device-verified on WebGPU; slangc v2026.12.2 segfaults differentiating the full tracePaths (both wgsl and hlsl targets) → needs a Slang release with the large-function autodiff fix |
| WhittedRayTracer | 🟡 | SoftwareRT, recursion → loop; verified byte-exact |

### 8.3 Ecosystem / tooling

| Component | Status | Notes |
|---|---|---|
| Mogwai app (functional viewer) | ✅ core | loads graph `.py` + `.pyscene`/`.pbrt`, per-frame execute, presents marked output (swapchain blit), play/pause + graph/output pickers, first-person camera, per-pass DOM `renderUI` panel, URL params. FrameCapture ✅ (Capture button: float outputs download as EXR, 8-bit as PNG). VideoCapture ✅ (Record button; §9: MediaRecorder WebM, frames pushed per presented frame like native). Python console ✅ (`m.scene`/`m.activeGraph`/`m.settings` bound to live state, expression echo + print capture, history; Playwright-verified live camera/material edits); canvas click-to-pick wired to PixelInspectorPass. TimingCapture ✅ (`m.timingCapture.captureFrameTime` — collects per-frame CPU ms, downloads on stop, §9: no file IO). Profiler panel ✅ (P key: per-event CPU/GPU table + history graph + capture download). Scene panel ✅ (camera, env map, lights, materials, Animate Scene — native Scene::renderUI subset; no render-settings toggles: the web Scene derives useEnvLight/useAnalyticLights/useEmissiveLights from content). Frame capture mirrors FrameCapture::captureOutput (`packages/mogwai/src/FrameCapture.ts`): one file per mask the output was marked with, single channels copied into a one-channel texture of the same bit depth (`.R/.G/.B/.A`), RGBA with alpha (`.RGBA`). `m.frameCapture` (graph scripts and the console) mirrors the FrameCapture extension: `addFrames(graph|name, frames)`, `capture()`, `reset`, `print`, `outputDir`, `baseFilename`, `captureAllOutputs`, `frameDigits`, `includeOutputInFilename`, `outputNameFilter`, with native's range rules and file naming, driven by the viewer's frame loop. §9: files download (and are kept in `captured`); `outputDir` only prefixes their recorded path; scripted `m.renderFrame()` loops (image-test helpers) are not driven from Python, since readback is asynchronous. `packages/mogwai/src/ScriptRunner.ts` runs unmodified Mogwai scripts headlessly: `recordMogwaiScript` executes the script and records `m.addGraph/loadScene/resizeFrameBuffer/renderFrame`, `m.clock`, `m.frameCapture`, `m.scene` edits and graph/pass calls after `addGraph` (e.g. `updatePass`, `g["X"].set_properties`, `getDictionary` round trips), then the runner replays them through Mogwai's renderFrame sequence (graphs sized from an sRGB target, as the swapchain FBO). All 34 portable `tests/image_tests/renderpasses/test_*.py`, the 12 `scene/` and the 5 `renderscripts/` image tests run end to end with native capture names (`--filter NativeImageTestScript`; DLSS/OptiX/SDFEditor/WARDiff are not portable). The sweep found and fixed: PathTracer kept the previous scene's LightBVH/power/env samplers after a scene switch (native `resetLighting`), `RenderGraph.updatePass` was missing, scene scripts had no `__file__` or local helper imports, four PBRT material factories were missing from the scene API, the Scene had a single camera (`m.scene.cameras`/`m.scene.camera = …`), FBX cameras were not imported, and Python pass properties (`SceneDebugger.mode = 'FrontFacingFlag'`) applied at record time instead of in script order. §9: script control flow must not depend on rendered results (true for the image tests) |
| Python scripting / console | 🔶 | Pyodide runs **unmodified** upstream `.py` graphs and `.pyscene` files via a curated `falcor` bridge (factories + SceneBuilderBridge + `AssetResolver`/`AssetCategory`/`SearchPathPriority`). Interactive console ✅ (viewer panel; `m.scene`/`m.activeGraph`/`m.settings`/`m.clock`/`m.profiler` on live state). ScriptWriter ✅ (`Utils/Scripting/ScriptWriter.ts`) with native's `getScript()` on Clock (now with exitTime/exitFrame and shouldExit, which SampleApp honours), Camera, Scene (render settings, `animated`, camera selection and pose, cameraSpeed) and FrameCapture; the viewer's **Save Config** button mirrors Mogwai's Renderer::saveConfig (graphs as exporter IR + m.addGraph, m.loadScene + scene script, window configuration, clock, frame capture), and `MogwaiSaveConfig.roundTrip` replays a saved config and gets the graph, camera, render settings and capture ranges back. No auto-generated ScriptBindings (the bridge is curated) |
| PyTorch interop (`falcor.pytorch`) | ❌ | no CUDA/torch in browser; ONNX-web-style substitute would be non-parity ⏳ |
| FalcorTest | 🔶 | vitest (unit) + Playwright GPU harness w/ native-oracle image compares (§7). Native GPU unit tests are being transplanted: `tests/gpu/harness/unit-test-context.ts` mirrors `GPUUnitTestContext` (createProgram, `vars()[...]`, allocateStructuredBuffer with WGSL-reflected strides, runProgram, readBuffer) and runs the native `Tests/**/*.cs.slang` kernels (served from `Falcor/Source/Tools/FalcorTest`) with native assertions, under `tests/gpu/suites/falcortest/`. Kernels needing WGSL changes get overrides in `WebFalcor/Overrides/Tests/` (typed buffers → structured, `InterlockedOr` → `Atomic<uint>`, `uint64_t` → `uint2`). Ported so far: HashUtils, BitTricks, MathHelpers (4), PackedFormats, LowDiscrepancy, Pseudorandom (3), HalfUtils (3 of 5; native disables the other two), AliasTable (with a port of `hypothesis::chi2_test` and of the `AliasTable` host class, `Utils/Sampling/AliasTable.ts`), RaySphereIntersection, GeometryHelpers (6 of 7; native disables BoxSubtendedConeAngleCenterRandoms), SampleGenerator (2), SlangMutating, Slang_Extension, SlangGenerics (4), NestedStructs, SlangEnum, SlangStructInheritanceLayout (checked at the WGSL layout: the inherited float3 sits at offset 16, native 4), Atomics_Buffer_InterlockedAddF32 (float atomics emulated by a compare-exchange loop), Microfacet (5, incl. chi^2 sampling over 24 NDF configurations), HairChiang16 (5, incl. the pbrt-v3 reference data), BSDFTests (8 BSDFs: chi^2 sampling plus sample/eval weight and pdf consistency), BufferTests (RawBuffer, TypedBuffer on a structured buffer, StructuredBuffer, BufferUpdate, BufferWrite; BufferWrite needed `Buffer.map()/unmap()`, now emulated for upload buffers with a CPU copy uploaded on unmap, which also lets upload buffers carry bind flags as natively), Core ConstantBuffer (2), ParamBlockCB, Texture_NestedArrays (fails as native does on Vulkan), RWTexture3D, TextureMinMaxMip, Texture2D_LoadMips (with the new `createTextureFromFile`/`createMippedTextureFromFiles`, `Core/API/TextureLoading.ts`, over Bitmap), Blit float/uint with and without filtering (blit now handles integer formats with an exact 2x2 floor-average, and takes native's copy fast path), BufferAccess (6; readback buffers are now bindable like natively and map through `Buffer.mapAsync()`), BufferAllocator (4, with a port of the `Utils/BufferAllocator` host class; it exposed that unaligned `Buffer.setBlob` zeroed up to 3 following bytes, since WebGPU copies need 4-byte multiples: unaligned updates on storage-capable buffers now merge the edge words in a compute pass, byte-exact as natively), Inheritance (ManualCreate, ConformanceCreate), DDSReadTests (20: BC1–BC7 incl. Tiny/Odd/Alpha variants, BC6HU16, BC7UnormBroken), RootBuffer / RootBufferStruct / RootBufferParamBlock (12; no root descriptors in WebGPU, so `[root]` buffers bind like the others; typed buffers become structured buffers, and the parameter block's resource arrays are unrolled into members, since WGSL has no arrays of resources), SlangDefaultInitializers (without its double case), SlangHashedStrings (with `ProgramReflection.getHashedStrings`), BufferAliasing_Read (ReadWrite is not portable: WebGPU rejects overlapping writable storage bindings in a dispatch), ShaderModel6_0..6_5 (one WGSL target), CopyColorChannel (15 renderable format pairs, with a port of `ImageProcessing`, `Utils/Image/ImageProcessing.ts`, writing through a fragment pass since most of those formats are not storage-capable in WebGPU; RGBA8Snorm and 16-bit unorm destinations are not portable), ShaderString (Inline, Module, Import, Imported, DynamicObject: `ProgramDesc.modules` now composes translation units from files and code strings, and named string modules are importable, as `ProgramDesc::addShaderModule().addString()`; entry points default to the last module that defines them, like native `csEntry`), WavelengthToXYZ (against the new host `SampledSpectrum`/`SpectrumUtils` port, which uses native's own 6-decimal CIE 1931 table), WaveOps (WaveGetLaneCount, WaveMinMax, WaveMaxSimpleInt, plus WaveMinMaxConditional and WaveMaxSimpleFloat, which native disables for compiler issues and which pass here). WaveOps exposed that WGSL's uniformity analysis rejects subgroup builtins in divergent branches, where HLSL wave ops act on the active lanes; the compiler now adds `diagnostic(off, subgroup_uniformity)` to modules that call them. HairChiang16's white furnace exposed that native masks a 0/0 at h = ±1 with `isnan()`, which WGSL compilers may fold away (WGSL lets implementations assume no NaN/Inf; even bit tests were folded here); the HairChiang16 override avoids the 0/0 instead. CPU_TESTs with a TS equivalent are in `packages/falcor/tests/falcortest/` (Settings, PathResolving, AssetResolver, Properties, Matrix, Quaternion, Spectrum, Float16Types, HalfUtils) and `packages/render-passes/tests/falcortest/` (ColorUtils: transforms and white balance, with the Bradford matrices and RGB↔XYZ helpers added from native), plus SampledSpectrum (`Utils/Color/SampledSpectrum.ts`, `Utils/Color/SpectrumUtils.ts`; the ports reproduce a C++ f32 build of native bit for bit, which also shows native's hard-coded `Y_D65 = 10567.0762` differs from what its formula gives, 10567.2959); they found two Settings gaps, now fixed: `getOption`/`getAttribute` convert to the default's type like `Attributes::get<T>` (a setting of `1` read as `false` before), and `addOptions` drops stale prefixed keys like `removePrefix`. Not portable: tests built on 16/64-bit scalar storage (Float16/Float64/Int64/CastFloat16/TemplatedLoad/SlangReinterpretCast/StructuredBufferMatrix/SlangScalarTypes; no `shader-f16` on this host, no 64-bit types in WGSL), fp16 and texture atomics, WaveMatch (no WGSL builtin), hardware-RT tests, and SplitBuffer (its shader side is an array of resources with 16-bit loads, and the Large cases need buffers over 2 GB). NestedStructs exposed that Slang's WGSL backend emits `bool` members in cbuffer/structured-buffer layout structs, which WGSL rejects; `Core/Program/WgslBoolLowering.ts` now lowers them to u32 after code generation (reads compare with zero, writes select 0/1), so upstream shaders with cbuffer bools compile unmodified and the TAA and Comparison overrides that existed only for this are gone. §9: native runs the last two HalfUtils/RaySphere tests on D3D12 only; here RaySphere's grazing and on-surface cases are checked against a strict-f32 replay of the shader, since native's analytic reference only holds under its compiler's rounding; ComputeRayOrigin accepts native's result or an exact single-rounding FMA per component, since native compiles it with FloatingPointModePrecise and WGSL can't forbid contraction (about a fifth of the results come out fused) |
| RenderGraphEditor (ImGui node UI) | 🔶 | basic node editor in the Mogwai viewer (Graph button): passes in dependency layers with input/output ports, edges as curves (`RenderGraph.getEdges()`), click-to-connect / click-to-remove, ★ marks graph outputs, add/remove passes; mid-edit graphs show the compile error in the status line instead of stalling; no drag layout or per-node property editing yet (the pass panel covers properties); Save graph .py downloads `RenderGraph.exportScript()` |
| RenderGraph `.py` export / RenderGraphIR | ✅ | `exportScript()` emits the camelCase image-test dialect (round-trip fixpoint verified); `removeEdge`/`unmarkOutput` added; divergence (§9): no snake_case IR, markOutput channel masks untracked. `markOutput(name, mask)` keeps native's channel masks (merging on re-mark, `"*"` marks every `getAvailableOutputs()` entry, `getOutputMasks`), and the exporter writes them |
| SampleApp (sample application base) | ✅ | `Core/SampleApp.ts` mirrors `SampleApp`: config (color/depth formats, headless, time scale, pause, UI), device + shader tree + font setup, target FBO, global clock and frame rate, text renderer, the onLoad/onResize/onFrameRender/onGuiRender/onKey/onMouse/onHotReload callbacks, native's frame sequence (tick, render or paused copy, UI, profiler, capture, present) and keyboard shortcuts, `renderGlobalUI`. Input events follow `Utils/UI/InputTypes` (`Utils/UI/InputTypes.ts`, native key names). §9: the window is a canvas presented each animation frame, the Gui a DOM panel (`Utils/UI/DomWidgets.ts`, shared with Mogwai) rebuilt on changes and twice a second, captures download, shader reload is F6, headless apps render when `renderFrame()` is called; PixelZoom and gamepads are not ported |
| Samples (Falcor/Source/Samples) | ✅ (CudaInterop ❌) | `packages/samples` (`npm run dev -w @web-falcor/samples`, `?sample=<Name>`): SampleAppTemplate, ShaderToy, Visualization2D (both demos, mouse-driven) and MultiSampling run unmodified sample shaders (MultiSampling drops an unused SV_PrimitiveID); HelloDXR loads Arcade and toggles (Space) between rasterization, which pulls vertices from the scene buffers (no SV_PrimitiveID in WGSL) and samples with explicit gradients (WGSL rejects implicit derivatives in the material code's non-uniform control flow), and ray tracing lowered to a compute kernel over the software BVH (primary, shadow and one reflection ray, the alpha-tested any-hit kept); it owns the first-person camera controller native Scene forwards input to. All five are checked headless in `samples.gpu.test.ts` (HelloDXR: raster and ray-traced primary visibility agree on every pixel, ray tracing adds shadows and reflections). MultiSampling §9: 4 samples in RGBA16Float (WebGPU multisamples at 1 or 4, not in RGBA32Float). Porting them found and fixed: `VertexBufferLayout.addElement` did not grow the stride (native does), the rasterizer's default front face was clockwise (native `mIsFrontCcw = true`: counter-clockwise triangles are front-facing, so a forced Back cull removed the front faces of CCW meshes), `blit` could not read multisampled sources (native's SAMPLE_COUNT > 1 averaging path), `RenderContext.clearFbo`/`createTexture2DMS`/`RasterPass.getState` were missing, and the pipeline cache ignored the vertex layout. CudaInterop needs CUDA |
| Testbed (Python-driven app) + Python device API | 🔶 | `Utils/Scripting/Testbed.ts` mirrors `Testbed` (frame: clock, clear, render graph + first output, render texture, FPS text, present; run, load_scene, create_render_graph, resize_frame_buffer, capture_output, show_ui, should_close) and `runTestbedScript` runs `falcor.Testbed` scripts unmodified (`packages/samples` page: `?testbed=/Falcor/scripts/python/balls/balls.py`). The script's `falcor` module has native names and keyword arguments: Device.create_buffer/_typed_buffer/_structured_buffer/_texture, Buffer/Texture from_numpy/to_numpy (native shapes and dtypes), ComputePass(file=, cs_entry=, defines=) with `globals`/execute (script shaders are compiled as string modules that import Falcor modules), profiler.event, RenderGraph create_pass/add_edge/mark_output, the resource enums, Logger. §9: Pyodide runs the script with JSPI, so `while not testbed.should_close: testbed.frame()` yields to the browser in frame() and to_numpy() waits on the GPU readback; numpy is provisioned by `scripts/setup-web.mjs` (pinned to the pyodide lock, sha256-checked); headless runs stop after `maxFrames`. Verified: balls.py (numpy-seeded compute simulation) renders. Not ported: `falcor.ui` (ui_demo.py), PyTorch (gaussian2d.py, test_pytorch.py), and test_replace_material.py (NeuralMaterial, whose assets this media drop lacks). Porting found two general gaps, now fixed: loose global uniforms (Slang's implicit globalParams buffer) were not settable, and RWTextures in non-r32 formats compiled to WGSL read_write storage textures, which WebGPU rejects; those only written now lower to write-only, which retired the overrides that existed only for that (Composite, CrossFade, ModulateIllumination, FLIPPass, VBufferRT, GBufferRT and PathTracer's ResolvePass now compile unmodified, with storage formats retargeted at bind time); likewise typed buffers (`Buffer<T>`/`RWBuffer<T>`, absent from WGSL) now lower to structured buffers at compile time, retiring the BakeIesProfile, EmissivePowerSampler and FLIP ComputeLuminance overrides and five FalcorTest ones, and modules outside the shader tree resolve Falcor imports |
| ImageCompare | ✅ | `Utils/Image/ImageCompare.ts` + `npm run image-compare -- [-l] [-m metric] [-t threshold] [-a] [-e heatmap] image1 image2` port the native tool: the mse/rmse/mae/mape metrics (native's mae squares the difference; kept), the error heat map, native's printing and exit codes. Images load the way the tool's direct FreeImage calls do, which differs from Bitmap: 3-channel floats (PFM, HDR, RGB EXR) are clamped to [0, 1] by `ConvertToRGBAF`, and PFM rows come in y-flipped (native Bitmap corrects that flip; the tool doesn't). Verified against the native binary on PNG/16-bit/palette/PFM/HDR/EXR inputs: same printed errors and exit codes, and byte-identical PFM heat maps (pixel-identical PNG; EXR heat maps are halves like FreeImage's default, within 4e-12). The GPU suites keep their inline gates |
| Importers | see §8.4 | glTF ✅ (TS), FBX 🔶 (assimpjs, `.fbx` full scenes only; cameras ✅ incl. animated ones via native's local view-matrix node and fixFbxCameraAnimation), PBRT ✅ subset (materials → Standard, or the PBRT material classes via `PBRTImporter:usePBRTMaterials`), `.pyscene` ✅, USD 🔶 subset (tinyusdz-wasm: meshes/xforms/UsdPreviewSurface incl. baseColor/ORM/normal/emissive textures, cameras and UsdLux lights, verified vs native — skel/subdiv ⏳), Mitsuba ✅ subset (upstream plugin's coverage; no Mitsuba content in the media drop, so the fixtures are spec-written scenes with closed-form answers rather than a native oracle) |
| SceneCache | ✅ | `Scene/SceneCache.ts` v5 (v5: camera list): OPFS binary cache keyed by SHA-256 of the scene source (§9: no file timestamps); geometry incl. skin/morph data + materials + textures (original compressed bytes) + env map (original .hdr/.exr bytes) + lights + camera + curves + node animations/weight tracks + SDF grids (rebuildable recipes: type/params/generator calls, regenerated deterministically on load) + grid volumes (NanoVDB buffers); cache-hit renders byte-identical (cornell, textured tutorial, two_curves, sphere_array, animated_cubes, NDSDF, smoke verified). Only programmatic env maps without source bytes stay uncacheable |
| Image IO (Bitmap/EXR read+write, image save) | ✅ | DDS as native `loadDDS`: BC1–BC7 incl. BC6H and BC4/BC5 SNORM, sRGB formats stored in the file stay sRGB, broken files return null; §9: BC textures not sized in whole blocks are allocated padded to a multiple of 4 (WebGPU requires it). All 19 DDSReadTests match native's reference images exactly, which native's own test never checked (its `diff` kernel compares the texture with itself). Material textures from DDS (`material.loadTexture(slot, 'x.dds')`, previously dropped) decode on the GPU for every BC format and match native's decode to ±1 (`MaterialDdsTexture`); `<MIP>` paths load their mip0 file (§9: the texture arrays generate their own mips; UDIM sets are not supported, and no Falcor content uses either). `.hdr`/DDS-BC/`.exr` decode (EXR via parse-exr, wired into ImageLoader + EnvMap + ErrorMeasure); DDS also parses plain 24/32-bit RGB(A) surfaces (channel-mask swizzle to RGBA8, e.g. the font atlas); `.tga` decode ✅ (`Utils/Image/TGADecoder.ts`: uncompressed and RLE true-colour, grayscale and colour-mapped, both origins — the web substitute for FreeImage, used by pbrt and FBX/OBJ textures); EXR write (uncompressed float scanlines, bit-exact round-trip) feeds the viewer capture; unified `Bitmap` ✅ (`Utils/Image/Bitmap.ts`: `create/createFromFile/createFromBytes`, `saveImage`, `getFormatFromFileExtension`, `getFileDialogFilters`, `getFileExtFromResourceFormat`, Export/Import flags) applying FreeImage's load rules — 8-bit gray → R8Unorm, 24-bit → BGRX8Unorm, 32-bit and palettes → BGRA8Unorm, gray-ramp palettes → R8Unorm, 16-bit gray → R16Unorm, 48/64-bit → RGBA16Unorm, float → RGBA32Float, all-half EXR or `ConvertToFloat16` → RGBA16Float — through its own PNG codec (all colour types and depths, Adam7) plus the browser for JPEG/BMP/GIF/WebP. Checked against fixtures from an independent writer (`scripts/gen-bitmap-fixtures.py`: Pillow plus a hand-rolled PNG writer); the encoders (PNG, TGA, BMP, PFM) read back identically in Pillow. `saveImage` keeps native's flag checks and conversions and writes PNG/JPEG/TGA/BMP/PFM/EXR; EXR does RGB or RGBA, half or float, uncompressed or ZIP. `Texture.captureToFile` mirrors native (returns the file, and downloads it in a page) and the viewer's frame capture goes through it like FrameCapture. §9: EXR_DEFAULT's PIZ and Lossy's B44 are written as lossless ZIP (half pixels identical); JPEG chroma subsampling is up to the browser; the caller's RGBA8 data is not swapped in place; 1/2/4-bit gray loads as R8 (native fails with "Unknown bits-per-pixel"); "Pf" PFMs load as R32Float (native reports BGRA8Unorm). Scene textures still decode to the 8-bit material texture arrays, since `rgba16unorm` isn't core WebGPU |
| NVTT texture compression | ❌ native / ⏳ substitute | decode side covered (DDS/BC parse + `texture-compression-bc` upload + CPU BC1/3/5 decode); a WASM BC *encoder* would be a substitute, not NVTT parity |

### 8.4 Scene, materials, lights, animation (audit 2026-07-09)

| Feature | Status | Notes |
|---|---|---|
| TriangleMesh geometry | ✅ | full path incl. instancing; alpha test ✅ with native semantics: the material header carries the base color texture as alpha texture handle (`Material::updateTextureHandle`) and the alpha mode follows `BasicMaterial::updateAlphaMode` — Mask only when the texture's alpha range (TextureAnalyzer parity via a CPU scan of the decoded image) can fall below the threshold; untextured materials never discard (native samples 1). Native has no Blend mode |
| DisplacedTriangleMesh / displacement mapping | ✅ core | full ray-marched displacement: displaced meshes get conservative per-tri AABBs in the merged BVH; upstream DisplacedTriangleMeshIntersector compiles via two overrides (max-shell-thickness — WebGPU lacks min/max-reduction samplers; Kahan diff-of-products replaces the fp64 watertight tiebreak). cornell_box_displaced FaceNormals match native (mean 5.1e-3; brick-edge march micro-divergence) and 256spp shaded radiance at bias 6.6e-4. v1: one displacement texture per scene |
| Curve geometry (LSS) / hair | ✅ | USD BasisCurves import (USDA text) → CurveTessellation port → linear-swept-sphere segments traversed via a segment-AABB BVH in the merged BVH buffer (zero extra bindings; verified footprint-invariant) → upstream CurveIntersector/Han19. two_curves.pyscene FaceNormals match native pixel-exactly (0/65536); shaded curves (native default HairMaterial, Chiang16) match native through MPT and the full PathTracer at 64spp (bias ≤1.1e-3). Poly-tube mode ✅: `CurveTessellation::convertToPolytube` ported (square cross sections carried by a rotation-minimizing frame, widths scaled by kMeshCompensationScale), chosen by `TessellateCurvesIntoPolyTubes` or a per-prim `curves:mode` Settings attribute, which is keyed by the full prim path as natively; `curves:subdivPerSegment` / `keepOneEveryXStrands` / `keepOneEveryXVerticesPerStrand` now apply to both modes. Verified on two_curves.usda: every poly-tube hit lies between the square section's inradius and circumradius, and swept-sphere hits sit at the curve radius to 4e-4. §9 improvement: upstream's Han19 swept-sphere intersector divides by (r2 − r1), so a segment of constant radius turns to inf/NaN and only its sphere joints hit — constant-width strands (common in pbrt hair) render as beads in native Falcor. The web override intersects such segments as exact cylinders (hits at the radius to 3e-8); segments whose radii differ keep the upstream path bit for bit. Those are the only two modes native has (CurveConfig.h's CurveTessellationMode), and `.hair` files are not a Falcor feature — this drop has no loader for them |
| SDF grids (NDSDF/SBS/SVS/SVO) | ✅ | all 4 representations GPU-verified. Content comes from the procedural generator or from `.sdfg` corner-value files (`Scene/SDFs/SDFGridFile.ts` mirrors `SDFGrid::loadValuesFromFile`/`writeValuesToFile`, reachable as `sdfGrid.loadValuesFromFile(path)` in a `.pyscene`); verified by rendering a generated analytic sphere, whose hits land on it to 1e-3 with normals to 6e-4. `.sdf` primitive files ✅ (`Scene/SDFs/SDF3DPrimitive.ts`: the JSON `SDF3DPrimitive` array SDFGrid.cpp reads and writes, all six shapes and six CSG operations, reachable as `sdfGrid.loadPrimitivesFromFile(path, gridWidth)`); the list stays on the grid afterwards, so it can be edited at runtime (see the SDFEditor row). §9: native runs `EvaluateSDFPrimitives.cs.slang` on the GPU and only SDFSBS accepts primitives; the port bakes that same kernel's corner values host-side, where its SDF build already lives, so every grid type takes them. Verified by rendering a generated CSG solid (sphere ∪ 45°-rotated bar, minus a sphere): hits sit on the analytic zero level set to 1.9e-4 median / 4.4e-3 at the 99th percentile, the bar lands on the diagonal the shader's transposed inverse rotation puts it on, and the subtraction leaves its dent |
| Custom primitives (procedural AABBs) | ✅ | as natively, a custom primitive is a user ID plus an AABB (`Scene.addCustomPrimitive/getCustomPrimitive/getCustomPrimitiveAABB/updateCustomPrimitive/removeCustomPrimitives`, `hasGeometryType(Custom)`, kept by the scene cache) for passes that bring their own intersection code. None of the shipped passes do: native PathTracer and MinimalPathTracer warn that they do not support custom primitives, and they occlude nothing — verified (the web used to draw them as box meshes with invented materials, which native never shows). §9: `SCENE_GEOMETRY_TYPES` omits the Custom bit since the software traversal never produces such hits. Rendering them needs an app-supplied intersection program, which only the TestRtProgram test pass has; it is ported |
| StandardMaterial | ✅ | verified across the oracle suite (incl. specular/normal-mapped pyscene materials — the tutorial G-buffer channel test pins normW/specRough/emissive vs native; a ~6% PT deficit found by the texLOD cross-oracle traced to the pyscene bridge dropping `addNode`'s parent argument, fixed). Both shading models: `StandardMaterial(name, ShadingModel.SpecGloss)` sets the flag bit and `MATERIAL_SYSTEM_HAS_SPEC_GLOSS_MATERIALS`, and `roughness`/`metallic` are refused in that mode with native's warning; a spec-gloss material (Ks 0.04, glossiness 0.75) gives the same G-buffer diffuse/specular/roughness as its metal-rough equivalent to 1e-4. The delta-specular header bit follows `updateDeltaSpecularFlag`. Texture slots follow each material class's slot table: slots a type lacks are ignored with native's warning, and the sRGB decision is per type (conductor base/transmission colours are linear, spec-gloss specular and transmission are sRGB); the transmission slot, previously dropped, now binds |
| PBRTConductor | ✅ | override shader + bridge |
| Cloth / Hair / PBRTDiffuse materials | ✅ | oracle-verified vs native (cloth-pt / hair-pt / pbrt-diffuse-pt / pbrt-conductor-pt suites); Hair additionally exercised by the curve scenes (Chiang16) — the old "unverified" marker was stale |
| PBRTDielectric / CoatedConductor / CoatedDiffuse / DiffuseTransmission | ✅ | instantiable via the native Settings key `PBRTImporter:usePBRTMaterials` (first web Settings consumer); one override (CoatedConductor instance aggregate-init vs explicit `__init`); verified vs native (`m.addOptions` oracle, 256-frame PT: per-material region means within 0.8%, bias 2.3e-3, 10/256 16×16 blocks) |
| MERL / MERLMix / RGL measured materials | ✅ | **MERL**: `Scene/Material/MERLFile.ts` reads the `.binary` database layout (90x90x180 half/difference bins, per-channel scales, inf/NaN sanitising) plus the optional `.json` fit; renders through the upstream `MERLCommon::eval` and `DiffuseSpecularBRDF` sampling. **RGL**: `Scene/Material/RGLFile.ts` reads the `tensor_file` container and rebuilds the samplable marginal/conditional CDFs (`SamplableDistribution4D`); the upstream `RGLCommon`/`RGLMaterialInstance` code renders unmodified apart from buffer addressing. Both are constructible from `.pyscene` (`MERLMaterial(name, path)`, `RGLMaterial(name, path)`) and integrate their albedo LUT with the same `BSDFIntegrator` native uses. §9: WGSL has no binding arrays, so all measured tables (one for MERL, eleven for RGL) and the float albedo LUTs share the single material buffer and the `*BufID` fields carry offsets into it. Verified — MERL against analytic BRDFs written in the MERL format by `scripts/gen-assets.mjs` (constant-albedo evaluation to 1.4e-8, integrated albedo exactly the fixture's, per-bin index probe exact); RGL against the real CC0 measurements (isotropic felt and an anisotropic butterfly wing): evaluation finite and non-zero across the hemisphere, reciprocal in the median (4e-2 / 2e-1 given 8 measured incident angles), and its own importance sampling reproduces the uniformly integrated albedo to 3-4 digits — the check that pins the host-built CDFs. **MERLMix**: several MERL BRDFs in one material, selected per texel by an index map (`MERLMixMaterial(name, paths)` + `loadTexture(MaterialTextureSlot.Index, path)`); the BRDF tables sit at a common stride in the shared buffer with the per-BRDF `DiffuseSpecularData` array and one stacked albedo LUT row per BRDF, each row integrated from a temporary single-BRDF scene exactly as `MERLFile::computeAlbedoLUT` does. §9: the index map lives in the material buffer too and the shader reproduces upstream's dedicated point/wrap sampler, because the packed texture array has only a linear sampler and interpolated BRDF indices are meaningless. Verified against three constant-albedo fixtures and a selector whose every texel is known: all 64 texels plus wrapped out-of-range uvs pick the right BRDF (1.3e-8), and each LUT row reports its own albedo (4e-7). |
| Texture LOD (ray cones / ray diffs) | ✅ core | material texture arrays carry full mip chains (layered blit-chain `generateMips`; material sampler = native trilinear + anisotropy 8) — previously every LOD mode silently sampled mip 0. GBufferRT `texLOD=RayCones` verified vs native (12× closer to the RayCones capture than Mip0; filtering detail is implementation-defined across APIs, §9); explicit-gradient path byte-exact; PathTracer `primaryLodMode` verified (Mip0/RayDiffs; RayCones warns → Mip0 like native): 2×2 cross-oracle — each web mode matches its native counterpart best and the web LOD-effect magnitude (3.13e-2) matches native's (3.0e-2); one override diff (runtime-selected ITextureSampler existential crashes the WGSL backend → single concrete gradient sampler under RayDiffs). Textures smaller than their array now sample like native: each tiles its layer (edge filtering wraps like repeat addressing, and for power-of-two sizes the layer's mips are the texture's own), ray-cone/ray-diff LODs use the texture's own size and explicit gradients are scaled into layer space; `TextureArrayLOD` checks that a texture renders bit-identically alone and next to a texture twice its size (ray cones, ray diffs, raster implicit LOD; the old layout was off by up to 0.25). §9: non-power-of-two textures that don't tile their layer exactly keep a `frac()` wrap |
| Analytic lights (Point/Directional/Distant/Rect/Disc/Sphere) | ✅ | verified incl. area-light sampling |
| Emissive geometry (LightCollection) | ✅ | incl. textured-emissive flux integration; animated emissive instances update the collection each frame their matrix changes (native `LightCollection::update` MatrixChanged; deformed/skinned emissives every frame) and bump `Scene.emissiveVersion`; LightBVH sampler ✅ incl. `lightBVHOptions` plumbing (sampler + builder options via the native nested-dict keys; BinnedSAH heuristic not ported — warns and uses BinnedSAOH; unit-tested tree structure + defines, end-to-end options oracle vs native); refit ✅ (`allowRefitting`, default on): native `LightBVHRefit.cs.slang` math ported to the CPU-resident tree and re-uploaded (§9.17), rebuild when the triangle set changes; RTXDI re-runs its light updater on emissive changes |
| Scene render settings (`RenderSettings`) | ✅ | `scene.renderSettings.{useEnvLight,useAnalyticLights,useEmissiveLights,useGridVolumes}` (python-compatible field names) AND-ed with resource presence in `Scene.use*` like native; the RenderGraph detects changes per frame (native `RenderSettingsChanged`) and re-sets the scene on its passes so kernels rebuild with the new light-usage defines; Mogwai Scene panel "Render Settings" group |
| EnvMap | ✅ | rotation/intensity/tint; loads Radiance `.hdr` and OpenEXR `.exr` (decodeExr) |
| LightProfile (IES) | ✅ | `Scene/Lights/LightProfile.ts` parses IESNA LM-63 photometry (supported profile identifiers, TILT=NONE, the 13-value header plus the angle and candela tables, optional normalization by max candela) and bakes it with the upstream `BakeIesProfile.cs.slang` into the 256² table the shader samples, with the flux factor reduced on the GPU. Reachable the native way: `sceneBuilder.loadLightProfile(path)` plus `material.lightProfileEnabled`, which drives `MATERIAL_SYSTEM_USE_LIGHT_PROFILE` and the MaterialSystem binding. §9: the bake target is R32Float (WGSL has no r16float storage format); the shader override swaps the typed `Buffer<float>` for a `StructuredBuffer<float>`. Verified against a CPU mirror of the bake kernel on generated profiles (`scripts/gen-assets.mjs`): texel-exact to 1e-7 and the flux factor matches (a cosine lobe integrates to π) |
| Camera (pinhole, jitter, motion vectors) | ✅ | verified (incl. prev-matrix roll). Camera list ✅ as natively: imported cameras first, then pyscene cameras, camera 0 selected unless `sceneBuilder.selectedCamera` is set; `Scene.getCameras/selectCamera` and the Python `scene.camera = scene.cameras[i]` (the new camera keeps the aspect ratio); the scene cache stores the whole list |
| Camera DoF / physical camera | ✅ | `apertureRadius`/`focalDistance` + thin-lens sampling verified vs native (VBufferRT depth/viewW 0 bad px; PathTracer consumes `viewW`); `shutterSpeed`/`ISOSpeed` plumbed (CameraData, pyscene `camera.shutterSpeed`, scene cache) — metadata only, like native |
| Camera controllers | ✅ | `Scene/Camera/CameraController.ts` ports Orbiter / FirstPerson / SixDoF incl. gamepad (dead zone + power curve + rotation/movement mapping, native math verbatim, unit-tested); the viewer drives these ported controllers through a DOM input adapter (normalized mouse pos, key letters + shift/ctrl, gamepad polled per frame; update(now) instead of CpuTimer) and exposes the native "Camera Controller" / "Up Direction" / "Camera Speed" controls in its Scene panel. §9: the Orbiter keeps the current view (target + distance/3.5) because the web Scene has no AABB yet; first-person wheel dolly is a web extra |
| Node / skinned / morph animation | ✅ | CPU skinning + morph (upstream does GPU skinning 🟡); camera/light animation ✅; LINEAR/STEP/CUBICSPLINE |
| Animated vertex caches | ⏳ | no `AnimatedVertexCache` support. Note this drop has no Alembic reader either — native fills its vertex caches from USD time samples (`ImporterContext` adds `CachedMesh`/`CachedCurve`), so the port needs tinyusdz to expose those samples before the GPU keyframe interpolation is worth building |
| Per-clip loop behaviors / global time control | ✅ core | time loops `fmod(t, length)` (AnimationController parity); per-clip pre/post-infinity behaviors (Constant/Linear/Cycle/Oscillate, set via `sceneBuilder.animations[i]` like native) verified vs native on animated_cubes — pre-infinity depth 0 bad px, hit coverage exactly equal; global time control ✅ via `m.clock` (Clock.ts) |
| Motion vectors for animated geometry | ✅ | rigid (prev world matrices) native-exact (mean 1.2e-7); skinned/morphed (prev-position double buffer + IsDynamic) verified by reprojection — native itself writes zero skinned mvecs on this content (probed) |
| Animated-scene BVH | ✅ | animated frames refit the software BVH (`refitBvh`: same topology and triangle order, bounds recomputed bottom-up, like native's BLAS updates) and rebuild once the refit tree's node surface area has doubled; on 131k triangles a refit takes 60 ms against 274 ms for a rebuild. Unit-tested for tight bounds and the rebuild fallback; hits are unchanged apart from equal-distance ties |
| GridVolumes (NanoVDB) | ✅ | `.vdb` parsed in-browser → NanoVDB, verified vs native. All OpenVDB value codecs read: COMPRESS_ACTIVE_MASK, ZIP and BLOSC (`Utils/Compression/`: a DEFLATE decoder, an LZ4 block decoder and the blosc1 container — the web substitutes for the zlib/blosc libraries native links through OpenVDB), half-float leaves, and linear maps that reduce to a uniform scale + translation (UniformScale/ScaleTranslate/Translation/diagonal Affine). openvdb.org's own sample volumes load and decode exactly: active-voxel counts match each file's `file_voxel_count` metadata, and the level-set sphere reproduces its analytic signed distance field through the GPU accessors (worst radius error 3e-5). Root tiles ⏳ (rejected loudly — the NanoVDB builder emits leaves only), `.vdb` frame sequences ✅ (`loadGridSequence(slot, paths, gridname)` plus frameRate/startFrame/playbackEnabled; the clock picks the frame with native's `(startFrame + floor(max(0,t) * frameRate)) % frameCount` and the viewer advances it — only one grid can be bound at a time (no binding arrays in WGSL), so a frame change re-uploads `gScene.grid0`; native's directory-scanning overload ❌, browsers cannot list directories), blackbody emission mode n/a (this Falcor drop stores the flag/temperature for the UI only — no shader consumes it) |
| Runtime material/light property edits | ✅ | `Scene.getLight/getMaterial` + `updateLights/updateMaterial` re-pack GPU data post-build; verified vs native applying identical python edits (mean 7.1e-4). Emissive edits rebuild the NEE flux tables (dimmed-cornell PT vs native: bias 2.5e-4, 0/1024 blocks). Emissive-presence toggles that flip scene defines still need pass recreation |
| Importer: glTF | ✅ | TS importer: meshes, skinning, morph targets, animations, cameras, lights; `KHR_mesh_quantization` ✅ (signed and normalized byte/short attributes, integer positions placed by the node transform) with `KHR_texture_transform` (baked into the primitive's uvs — each primitive has one material, so this is exact); `KHR_draco_mesh_compression` ✅ via Google's `draco3d` decoder (loaded like the other wasm deps; native gets Draco through assimp). Both verified against the same model's uncompressed variant: over 90% of the shaded surface agrees to 7e-5 / 5e-4 in position and 0.2° / 0.5° in normal. KTX2/`KHR_texture_basisu` ❌ out of scope: this Falcor drop has no KTX2 or Basis Universal reader either, so supporting it would put the port ahead of native rather than at parity |
| Importer: Assimp | 🔶 partial | assimpjs: full-scene `importScene` for every extension AssimpImporter registers (glTF, USD and pbrt keep their own importers, as natively). OBJ gets native's OBJ mode: spec-gloss materials (unless `UseMetalRoughMaterials`), Phong exponent → glossiness, bump/displacement maps as the normal slot, and its `mtllib` files fetched for assimp. `UseSpecGlossMaterials` applies to the other formats. Only materials a mesh uses enter the scene, in first-use order, like native's addMesh (assimp's unused `DefaultMaterial` no longer leaks in). DDS ✅ and TGA ✅ textures (TGA was previously skipped on this path). Verified with spec-written OBJ/MTL fixtures: the MTL material shades exactly like its metal-rough equivalent, and a TGA map decodes to its sRGB-linear value. >2 GB-heap FBX aborts (wasm32) — BistroExterior; `DontMergeMeshes` cannot reach assimpjs's fixed post-process flags |
| Importer: PBRT (pbrt-v4) | ✅ subset | camera/lights/shapes/area lights verified; materials → Standard by default, or the dedicated PBRT classes via the `PBRTImporter:usePBRTMaterials` Settings option (native key; area lights stay Standard). `Texture` directives ✅: named `imagemap` textures bind to the material slot (diffuse/coateddiffuse reflectance, plus the material-level `normalmap`), `constant` and `scale` textures fold into constants, and the rest warn like native. pbrt scenes ship TGA images, which browsers cannot decode — `Utils/Image/TGADecoder.ts` covers that (see Image IO). Verified by rendering a textured quad: 13918/13924 shaded pixels equal an exact bilinear mip-0 fetch of the decoded image. Note (matches native): pbrt's own renderer flips v in image lookups; Falcor passes pbrt uvs through unchanged, so textured pbrt scenes are v-mirrored relative to pbrt itself in both ports. Spectra ✅: named (`metal-Au-eta`, `stdillum-D65`, `glass-BK7`, … — all 88 of native's, generated from its Spectra.inl by `scripts/gen-spectra.mjs`), sampled and blackbody parameters go through `Utils/Color/Spectrum.ts` (CIE 1931 curves, Rec.709 matrix, native's float32 1 nm inner product); native's defaults follow (copper conductor, D65 distant light). Infinite lights take native's two paths: a constant `L` becomes a one-pixel env map, a `filename` an equal-area-octahedral map converted to lat-long (`Scene/Lights/EnvMapConverter.ts`, §9: on the CPU, since the kernel's partial store to an RGBA32F storage texture is not expressible in WGSL) and oriented by the light's transform; `.pfm` env maps decode (`Utils/Image/PFMDecoder.ts`). Diffuse area lights ignore `scale`, as natively. Verified: metal-Cu eta reproduces the RGB previously hard-coded from native to 3e-4, D65 is white to 2e-4, and an octahedral split-hemisphere map lands on the right halves of the lat-long map. `curve` shapes ✅: strands aggregate per (transform, material) with native's width interpolation over i / pointCount and `splitdepth` → 2^depth subdivisions, emitted as swept-sphere curves or, under `TessellateCurvesIntoPolyTubes`, poly-tube meshes; `runPbrtScene` now takes builder flags. Procedural textures and media are not gaps: native only implements `constant`/`imagemap` textures and warns on every medium type |
| Importer: `.pyscene` | ✅ | unmodified upstream scenes via Pyodide bridge |
| Importer: USD | 🔶 subset | tinyusdz-wasm (1.9MB, reads usda/usdc/usdz): meshes + xform hierarchy + UsdPreviewSurface → Standard incl. UsdUVTexture baseColor (sRGB, V-flip) and roughness/metallic packed ORM like native's CreateSpecularTexture — verified vs native (mask/viewW exact; textured 64spp radiance bias ~1e-5, per-region ≤0.3%). Normal/emissive texture slots plumbed (same path, not separately oracled). Opacity follows PreviewSurfaceConverter: uniform opacity → specular transmission 1 − opacity, opacity under an `opacityThreshold` → an alpha cutout (textured opacity packed into the base colour's alpha, like packBaseColorAlpha), textured opacity without a threshold → a grey transmission texture of 1 − opacity (createSpecularTransmissionTexture); every material is double-sided and specular alpha is 1, as natively; displacement textures bind; `useSpecularWorkflow` warns. Verified on a four-quad fixture, including native's quirk that an *untextured* cutout is never discarded (the alpha test reads the texture's alpha). Cameras and lights ✅: tinyusdz's RenderScene has neither, so `Scene/Importer/UsdaScene.ts` reads them from the layer's USDA text (`layerToString` for usdc/usdz), with xformOp stacks through the prim hierarchy, and mirrors ImporterContext (UsdGeomCamera pose, focal length, f-stop aperture, clipping range and focus distance in meters, film width or height; Distant/Sphere/Rect/Disk lights with native's scalings and 2^exposure·intensity·color, DomeLight → env map with native's rotation); the stage root transform (scale(metersPerUnit), Z-up → Y-up) now applies to meshes as natively. The USD oracle test now loads the .usda alone, like native's script (mask exact, viewW 9.5e-5). ⏳: color temperature, texture channel selectors (tinyusdz's texture objects carry only an image id and wrap modes, so red is assumed), UsdSkel, subdivision refinement, instancing |
| Importer: Mitsuba | ✅ subset | `Scene/Importer/MitsubaImporter.ts` mirrors the upstream plugin's coverage: sensors (perspective/thinlens), shapes (obj, ply, sphere, disk, rectangle, cube), BSDFs (diffuse, conductor, roughconductor, dielectric, roughdielectric, thindielectric, plastic, roughplastic, twosided), emitters (area, constant, envmap), textures (bitmap, checkerboard), homogeneous interior media, and the translate/rotate/scale/lookat/matrix transform ops, including the pre-2.0.0 tree upgrade. `.xml` scenes load in the viewer like `.pbrt` and `.pyscene`. Upstream's own limits are mirrored rather than fixed: `<default>`/`<include>`/`<alias>`/`<path>` are rejected as "not implemented", `<spectrum>` is read with the RGB parser, `point` emitters are dropped, and `<matrix>` reads its value list column-major (a transpose of Mitsuba's row-major convention). §9: `twosided` marks the *inner* material double-sided (upstream dereferences a null material there), per-texture `to_uv` transforms warn instead of applying, and the procedural checkerboard is RGBA8. The drop ships no Mitsuba content, so verification uses spec-written fixtures with closed-form answers: a diffuse rectangle under a constant environment renders albedo × radiance to 4 digits, a bitmap-textured one matches the decoded texture's mean albedo to 0.6%, checker quadrants land on their exact colours, env-map `scale` is exact, transform chains reproduce the predicted world bounds, and every BSDF/sensor/emitter parameter is checked against upstream's mapping |
| SceneBuilder flags (optimize/merge/dedup) | ✅ core | `SceneBuilderFlags` (native values) via `runSceneScript(..., {flags})` and python `SceneBuilderFlags`; honoured: `AssumeLinearSpaceTextures` (pyscene + FBX/glTF/USD texture loads), `DontUseDisplacement`, `UseOriginalTangentSpace` (builder meshes), `UseCache`/`RebuildCache` (OPFS cache, flags part of the key); accepted no-ops because the web already behaves that way: `DontMergeMaterials`/`DontOptimizeMaterials`/`DontOptimizeGraph` (no merging/optimisation passes), `Force32BitIndices` (always 32-bit), `FlattenStaticMeshInstances` (imports flatten), `UseCompressedHitInfo` (uncompressed), `RTDontMerge*` (software BVH); `UseSpecGlossMaterials`/`UseMetalRoughMaterials` (assimp import shading model, mutually exclusive as natively); `NonIndexedVertices` (every triangle gets its own vertices, skin and morph data expanded alongside; §9: an identity index buffer stands in for native's unindexed draw, addressing the same vertices — the G-buffer is byte-identical), `TessellateCurvesIntoPolyTubes` (see the curves row); `DontMergeMeshes` ⏳ accepted-but-ignored, since assimpjs's post-process flags are fixed |

### 8.5 Framework, debugging & test infrastructure

| Feature | Status | Notes |
|---|---|---|
| RenderGraph core (compile, alloc, I/O merge, `.py` load) | ✅ | verified across all feature graphs |
| RenderPassReflection completeness | ✅ | full `Field` API (`rawBuffer`/`texture1D`/`texture2D`/`texture3D`/`textureCube`/`resourceType`, `kMaxMipLevels`, `name`/`desc`, `isValid`, native `merge` conflict errors + `operator==`); allocation mirrors `ResourceCache::createResourceForPass` (raw buffers, 1D/3D/cube/MSAA textures, size-0 → graph dims, Unknown → graph default format, `None` bind flags resolved from the format's WebGPU capabilities via `Device.getFormatBindFlags`); external inputs surface in `connectedResources`; `Persistent` fields keep their resource across recompiles while unchanged (§9). Web rules: 1D outputs never get render-target usage; UAV-bound r8/r16uint promote to r32uint |
| PixelDebug host (shader `print()` readback/console) | ✅ | `Utils/Debug/PixelDebug.ts` + portable override (Atomic counters + flat record buffer replace the ParameterBlock/UAV-counter layout): print/assert records captured per selected pixel, typed decode verified end-to-end; wired into the passes that own one natively — PathTracer (TracePass), SceneDebugger, BSDFViewer, RTXDI (candidate generation + spatial resampling) — with the native "Debugging" group (enable toggle recompiles with `_PIXEL_DEBUG_ENABLED`, selected pixel, log) and `onMouseEvent` left-click selection; message strings resolve through the program's hashed-string reflection (`ProgramReflection.getHashedStrings`, as native's `mHashToString`; verified for print and assert messages); §9: async readback |
| PixelStats | ✅ | packed-atomic-buffer collection (verified vs native rayCount/pathLength dumps) + `Rendering/Utils/PixelStats.ts` `getStats()` aggregate (GPU region sums, async readback ~1 frame late, docs §9); PathTracer exposes `getPixelStats()` |
| WarpProfiler | ✅ | `Utils/Debug/WarpProfiler.ts` (bindShaderData, begin/end, getWarpHistogram, saveWarpHistogramsAsCSV returning the text; readback async) over WebGPU subgroups. The shader override keeps the native API: the histogram is an `Atomic<uint>` buffer, WaveMatch (no WGSL builtin) is a WaveReadLaneFirst/ballot loop, and WaveActiveCountBits is countbits of the ballot. Needs 32-wide subgroups, as native assumes. The transplanted FalcorTest (native: D3D12 only) reproduces its exact utilization and divergence counts |
| BSDFIntegrator (white furnace) | ✅ | `Rendering/Materials/BSDFIntegrator.ts` + one override (wave reduction → shared-memory tree, no `= {}` ShadingData); upstream FalcorTest values reproduced (four incident angles, rough dielectric StandardMaterial); async readback (§9) |
| Algorithm library | ✅ | ParallelReduction ✅, PrefixSum ✅, BitonicSort ✅ (portable shared-memory override of the NVAPI warp-shuffle kernel, §9; exact vs CPU chunk sort incl. 2D dispatch + tail padding) |
| Utils/Math | ✅ | Vector/Matrix/Quaternion ✅; CubicSpline host port (`Utils/Math/CubicSpline.ts`, drives curve tessellation, unit-tested); `SphericalHarmonics.slang` compiles to WGSL — 16 basis functions GPU-verified against a CPU transcription |
| Gui (Dear ImGui) | ✅ (DOM) | DOM `UIWidgets` (text/button/checkbox/slider/dropdown/group); `renderUI` ported on 26 of 28 pass classes (native controls via text/button/checkbox/slider/dropdown/group; "Output size"/"Size in pixels" wired through `RenderPass.requestRecompile()`; InvalidPixelDetection and RenderPassTemplate have no controls natively); TextRenderer/Font ✅ (`Utils/UI/`: upstream `dejavu-sans-mono-14.bin/.dds` atlas fetched from the served Falcor tree, upstream `TextRenderer.3d.slang` via a struct-varyings override, SrcAlpha blending — §9.16); PixelZoom ✅ in the viewer (hold Z: 200² point-filtered window of the 5² block under the cursor, wheel ±4 px, native placement/clamping; 2D overlay canvas drawn from the WebGPU canvas after present) |
| Video (FFmpeg encode/decode) | ❌ native / 🔶 substitute | `Utils/Video/VideoRecorder` records the presented canvas per rendered frame (MediaRecorder over `canvas.captureStream` + `requestFrame`, WebM VP9/VP8 as the browser offers, constant quality) behind the viewer Record button — the Mogwai VideoCapture role; FFmpeg codecs/containers, decode and a WebCodecs encoder ⏳ |
| AssetResolver | ✅ | `Core/AssetResolver.ts`: per-category search paths (Any/Scene/Texture, First/Last priority, category → Any fallback), `getDefaultResolver` seeded with `/Falcor/media` (SampleApp's project media dir); `.pyscene`/`.pbrt` loads push the script directory first and restore afterwards (Mogwai `loadScript`); SceneBuilder imports/`createFromFile`/`loadTexture`/EnvMap/Grid, ImageLoader and ErrorMeasure resolve through it; python `AssetResolver.default_resolver.add_search_path`/`resolve_path` + `AssetCategory`/`SearchPathPriority`. §9: async (HTTP HEAD existence probe), no cwd-relative lookup, `resolvePathPattern` (UDIM/`.vdb` sequence globbing) ❌ — no directory listing |

## 9. Known behavioral divergences (accepted, documented)

1. **Async boundaries.** Device creation, shader compilation, buffer readback and
   screenshot capture are `async` on the web (native Falcor blocks). APIs that are
   synchronous in Falcor and *cannot* be async-hidden return Promises; graph execution
   itself stays synchronous per-frame (encoders are synchronous).
2. **No raw pointers/interop handles.** `getNativeHandle()` returns the WebGPU object.
3. **Float determinism.** WGSL→driver compilation differs from DXIL/SPIR-V; image
   tests use tolerance thresholds, not bit-exactness (same policy as Falcor's own
   cross-vendor tests).
4. **Performance envelope.** Software RT and no SER/bindless put the ceiling below
   native; parity target is *feature/semantics*, not frame-time.
5. **Animated scenes rebuild the CPU BVH each frame** (no refit); results are
   correct but scale linearly with geometry, unlike native BLAS refit.
6. **BC-textured scenes currently sample CPU-decoded RGBA8 (≤512px per texture)**
   instead of native full-res BC arrays — a quality/memory divergence until the
   per-format BC texture-array material path lands (⏳, §8.4).
7. **Displacement marching uses the conservative global shell** (max thickness)
   instead of native's per-triangle min/max-sampler tightening — WebGPU has no
   reduction samplers (§8.1). The shell only bounds the search range, so
   intersections are identical; marching is somewhat slower. v1 also binds a
   single displacement texture per scene.
8. **The fp64 watertight-triangle tiebreak is replaced by a Kahan
   difference-of-products (`fma`)** — extended precision rather than true fp64;
   affects only exactly-edge-grazing rays.
9. **Custom primitives render as visible box meshes** (the 🟡 approximation in
   §8.4); native draws nothing for them unless an app supplies an intersection
   shader. Scene compares strip `addCustomPrimitive` calls.
10. **The 16-storage-buffer-per-stage cap shapes bindings.** This Chromium's
   per-stage limit is 16 even at the adapter maximum. Small read-only tables
   (RTXDI neighbor offsets, per-texture uv-scale info) live in 1-row textures
   behind buffer-style `__subscript` wrappers. The PathTracer+RTXDI megakernel
   sits exactly at 16; combining USE_RTXDI with pixel stats or curve scenes
   would exceed it (not currently co-usable).
11. **`Persistent` reflection fields outlive recompiles.** Native only promises
   the resource is stable between `execute()` calls; the web graph recompiles far
   more often (resize, scene change, pass edits), so a `Persistent` field also
   keeps its resource and contents across recompiles while its resolved
   description (type, dims, format, flags) is unchanged. Graph default format
   for `Unknown` outputs stays `RGBA32Float` (native: swapchain format) unless
   `onResize(w, h, format)` supplies one.
12. **Asset resolution is asynchronous and URL-based.** `AssetResolver.resolvePath`
   probes candidates with HTTP HEAD (GET-range fallback; a dev server's
   `index.html` fallback does not count as a hit) and returns a Promise; native
   returns synchronously from the filesystem. There is no working-directory
   lookup step and no `resolvePathPattern` (browsers cannot list directories),
   so `<UDIM>` texture sets and `.vdb` frame sequences stay unsupported.
13. **Animation clip behaviors match native** (Constant default; per-clip
   Linear/Cycle/Oscillate honored, §8.4); the Linear edge-slope extrapolation
   quantizes at f32 keyframe precision on both sides, so it is
   tolerance-compared like all float outputs.
14. **Raster G-buffer passes cull nothing unless `forceCullMode` is set** (native
   defaults to back-face culling) so raster and software-RT coverage agree; the native
   `forceCullMode` / `cull` options are honoured when given.
15. **Raster G-/V-buffer "extra" channels are render targets, not ROV stores.** WebGPU has
   no rasterizer-ordered views and no depth-gated fragment UAV writes, so GBufferRaster
   assigns every connected channel a batch + `LOC_<name>` location and draws the scene
   once per batch of ≤8 targets (each batch re-resolves depth with Less+write; identical
   draw order and vertex math keep batches pixel-consistent). VBufferRaster adds mvec/mask
   as SV_TARGET1/2. The GBufferRaster `vbuffer` channel needs SV_PrimitiveID, so connecting
   it switches the pass to non-indexed vertex pulling (like VBufferRaster) for all batches.
   Also: upstream declares raster varyings HLSL `linear` (perspective-correct),
   which the WGSL backend emits as `@interpolate(linear)` (screen-space); the web
   `Scene/Raster.slang` override drops the qualifier (perspective default) — the
   previous mapping skewed posW/normals on tilted surfaces.
16. **TextRenderer loads its font asynchronously and blends only where WebGPU allows.**
   The upstream atlas (`data/framework/fonts/dejavu-sans-mono-14`) is fetched from the
   served Falcor tree, so `render()` is a no-op until `init()` resolves (passes with
   labels enabled await it in `initAsync`). SrcAlpha/OneMinusSrcAlpha blending on 32-bit
   float targets needs the `float32-blendable` feature (requested when available);
   without it glyph quads draw opaque.
17. **LightBVH refit runs on the CPU.** The web tree is CPU-built and CPU-resident, so
   the native `LightBVHRefit.cs.slang` leaf/internal math is executed on the host and
   the packed nodes re-uploaded, instead of the per-depth GPU kernels. Native quirk kept
   on both sides: the builder invalidates cones of single-triangle / identical-normal
   leaves (`sinTotalTheta == 0`) while the refit yields the tight cone.
